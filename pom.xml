<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.nightlabs.jfire.eclipse</groupId>
	<artifactId>org.nightlabs.jfire.base.j2ee.osgi</artifactId>
	<packaging>bundle</packaging>
	<name>${project.artifactId}</name>

	<parent>
		<groupId>org.nightlabs.jfire.eclipse</groupId>
		<artifactId>org.nightlabs.jfire.eclipse.parent</artifactId>
		<version>1.2.0-SNAPSHOT</version>
		<relativePath>../org.nightlabs.jfire.eclipse.parent/pom.xml</relativePath>
	</parent>

	<dependencies>
		<dependency>
			<groupId>org.eclipse.osgi</groupId>
			<artifactId>org.eclipse.osgi</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.nightlabs</groupId>
			<artifactId>org.nightlabs.classloader.delegating</artifactId>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<build>
		<sourceDirectory>src</sourceDirectory>
		<outputDirectory>bin</outputDirectory>
		<resources>
			<resource>
				<directory>src</directory>
				<excludes>
					<exclude>**/*.java</exclude>
				</excludes>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<extensions>true</extensions>
				<inherited>false</inherited>
				<configuration>
					<!-- We deactivated the dependency-embedding in the parent-pom, but need it here. Hence,
					overriding the excludeDependencies here. Marco :-) -->
					<excludeDependencies>*;scope=runtime</excludeDependencies>

					<instructions>
						<!-- the provide:=true instruction results in having Import-Package 
							with version range e.g. "[1.2,1.3)" instead of "[1.2,2)" which is the default. 
							Applies to all org.nightlabs packages. See http://www.aqute.biz/Bnd/Versioning -->
						<Import-Package>org.nightlabs.*;provide:=true,*</Import-Package>
						<Export-Package>
						org.nightlabs.classloader.delegating;version="${project.version}",
						org.nightlabs.classloader.osgi;version="${project.version}",
						org.nightlabs.jfire.base.j2ee.osgi;version="${project.version}"
						</Export-Package>
<!--						<Private-Package>${bundle.namespace}.internal.*</Private-Package> -->
						<Bundle-RequiredExecutionEnvironment>JavaSE-1.6</Bundle-RequiredExecutionEnvironment>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>